apiVersion: v1
kind: Service
metadata:
  name: bulba-baby-service
  namespace: signal-town
spec:
  type: ClosterIP
  selector:
    app: bulba-baby-wrong
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
